import { SiteDict } from "./types";

// Temporary scaffold — populated field-by-field in Task 6.
// The `as SiteDict` cast is intentional here and is removed once every
// field required by SiteDict is filled in.
export const pt: SiteDict = {
  locale: 'pt',
} as SiteDict;
